# 🏰 Castle Defense - 아키텍처 문서

## 📋 개요

Castle Defense는 HTML5 Canvas 기반의 타워 디펜스 게임입니다. 순수 JavaScript ES6 모듈 시스템을 사용하여 객체지향적으로 설계되었습니다.

---

## 📁 디렉터리 구조

```
castle-depense/
├── index.html              # 메인 HTML 진입점
├── style.css               # 전역 스타일시트
├── start.bat               # 로컬 서버 시작 스크립트
├── README.md               # 프로젝트 설명
│
├── docs/                   # 📚 문서
│   ├── architecture.md     # 아키텍처 문서 (현재 파일)
│   ├── requirements.md     # 게임 요구사항
│   ├── implementation_plan.md    # 구현 계획
│   ├── parallel_implementation_plan.md  # 병렬 구현 계획
│   ├── task.md             # 작업 목록
│   ├── ui_design.md        # UI 디자인
│   └── implementation_agent.md   # AI 에이전트 구현 가이드
│
├── js/                     # 🎮 JavaScript 소스
│   ├── main.js             # 애플리케이션 진입점
│   ├── Game.js             # 메인 게임 루프 및 상태 관리
│   ├── constants.js        # 게임 상수 정의
│   ├── utils.js            # 유틸리티 함수
│   │
│   ├── entities/           # 🧩 게임 엔티티
│   │   ├── Entity.js       # 기본 엔티티 클래스 (Base)
│   │   ├── Castle.js       # 성 클래스
│   │   ├── Monster.js      # 몬스터 클래스
│   │   └── Projectile.js   # 투사체 클래스
│   │
│   └── managers/           # 🔧 매니저
│       ├── SpawnManager.js # 몬스터 소환 관리
│       └── UIManager.js    # UI 업데이트 관리
│
└── test/                   # 🧪 테스트
    ├── test_runner.html    # 브라우저 테스트 러너
    └── entities/           # 엔티티 테스트
        ├── Castle.test.js
        ├── Monster.test.js
        └── Projectile.test.js
```

---

## 🏗️ 클래스 다이어그램

### 상속 구조

```
                    ┌─────────────┐
                    │   Entity    │  (Base Class)
                    │─────────────│
                    │ + x, y      │
                    │ + width     │
                    │ + height    │
                    │ + color     │
                    │ + team      │
                    │ + isDead    │
                    │─────────────│
                    │ + update()  │
                    │ + draw()    │
                    └──────┬──────┘
                           │
         ┌─────────────────┼─────────────────┐
         │                 │                 │
         ▼                 ▼                 ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│     Castle      │ │     Monster     │ │   Projectile    │
│─────────────────│ │─────────────────│ │─────────────────│
│ + hp, maxHp     │ │ + grade         │ │ + targetX/Y     │
│ + attackRange   │ │ + type          │ │ + damage        │
│ + attackDamage  │ │ + hp, maxHp     │ │ + speed         │
│ + attackCooldown│ │ + speed         │ │ + directionX/Y  │
│ + currentCooldown│ │ + attackDamage │ │─────────────────│
│─────────────────│ │ + attackRange   │ │ + update()      │
│ + takeDamage()  │ │ + state         │ │ + hasReached()  │
│ + heal()        │ │─────────────────│ │ + draw()        │
│ + canAttack()   │ │ + move()        │ └─────────────────┘
│ + attack()      │ │ + attack()      │
│ + isInRange()   │ │ + takeDamage()  │
│ + draw()        │ │ + findTarget()  │
└─────────────────┘ │ + update()      │
                    │ + draw()        │
                    └─────────────────┘
```

### 매니저 클래스

```
┌─────────────────────┐     ┌─────────────────────┐
│      UIManager      │     │    SpawnManager     │
│─────────────────────│     │─────────────────────│
│ - playerHpEl        │     │ - game (Game ref)   │
│ - playerEnergyEl    │     │ - aiLevel           │
│ - aiHpEl            │     │ - aiSpawnTimer      │
│ - timerEl           │     │ - playerDeck[]      │
│ - deckContainer     │     │ - costs{}           │
│ - gameOverScreen    │     │─────────────────────│
│─────────────────────│     │ + setAILevel()      │
│ + createDeckUI()    │     │ + update()          │
│ + updateDeckUI()    │     │ + spawnAIMonster()  │
│ + updateGameUI()    │     │ + spawnPlayerMonster()│
│ + showGameOver()    │     │ + getDeckInfo()     │
│ + showStartScreen() │     └─────────────────────┘
└─────────────────────┘
```

---

## 🔄 데이터 흐름

### 게임 초기화 흐름

```
[DOM Ready]
     │
     ▼
┌─────────┐     ┌─────────┐
│  Main   │────▶│  Game   │
└─────────┘     └─────────┘
     │               │
     ▼               ▼
┌─────────────┐  ┌──────────┐
│ SpawnManager│  │  Canvas  │
└─────────────┘  └──────────┘
     │
     ▼
┌─────────────┐
│  UIManager  │
└─────────────┘
     │
     ▼
[Show Start Screen]
     │
     ▼
[User Selects AI Level]
     │
     ▼
[Game.start()]
```

### 게임 루프 흐름

```
┌────────────────────────────────────────────────────────────┐
│                    Game Loop (60 FPS)                      │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  1. Calculate Delta Time                                   │
│       │                                                    │
│       ▼                                                    │
│  2. Update Timer & Energy                                  │
│       │                                                    │
│       ▼                                                    │
│  3. Update Castles (Crystal Attack Logic)                  │
│       │                                                    │
│       ▼                                                    │
│  4. Update Monsters (Movement & Attack)                    │
│       │                                                    │
│       ▼                                                    │
│  5. Update Projectiles (Movement)                          │
│       │                                                    │
│       ▼                                                    │
│  6. Check Collisions (Projectile ↔ Monster)                │
│       │                                                    │
│       ▼                                                    │
│  7. Remove Dead Entities                                   │
│       │                                                    │
│       ▼                                                    │
│  8. Check Win Condition                                    │
│       │                                                    │
│       ▼                                                    │
│  9. Render (Draw All Entities)                             │
│       │                                                    │
│       ▼                                                    │
│  10. Update UI (via onUpdate callback)                     │
│       │                                                    │
│       ▼                                                    │
│  requestAnimationFrame() ──────────────────────────────┐   │
│                                                        │   │
└────────────────────────────────────────────────────────│───┘
         ▲                                               │
         └───────────────────────────────────────────────┘
```

---

## 📊 주요 모듈 책임

### 1. `main.js` - 애플리케이션 진입점
| 책임        | 설명                               |
| ----------- | ---------------------------------- |
| 초기화      | DOM 로드 후 게임 인스턴스 생성     |
| 의존성 주입 | Game, SpawnManager, UIManager 연결 |
| 콜백 설정   | 게임 이벤트와 UI 이벤트 연결       |
| 게임 시작   | AI 레벨 선택 후 게임 시작          |

### 2. `Game.js` - 게임 핵심 로직
| 책임        | 설명                                    |
| ----------- | --------------------------------------- |
| 게임 루프   | `requestAnimationFrame` 기반 60FPS 루프 |
| 상태 관리   | 타이머, 에너지, 승/패 상태              |
| 엔티티 관리 | 성, 몬스터, 투사체 컬렉션 관리          |
| 충돌 감지   | 투사체와 몬스터 충돌 체크               |
| 렌더링      | 모든 엔티티를 캔버스에 그리기           |

### 3. `entities/Entity.js` - 기본 엔티티
| 책임        | 설명                           |
| ----------- | ------------------------------ |
| 공통 속성   | 위치, 크기, 색상, 팀, 생존상태 |
| 기본 렌더링 | 사각형 + 팀 표시 테두리        |

### 4. `entities/Castle.js` - 성 엔티티
| 책임          | 설명                             |
| ------------- | -------------------------------- |
| 체력 관리     | HP, maxHP, 데미지/회복           |
| 크리스탈 공격 | 범위 내 적 탐색 및 공격          |
| 쿨다운 관리   | 공격 쿨다운 타이머               |
| 시각적 표현   | 성 본체, 성벽, 문, 체력바 렌더링 |

### 5. `entities/Monster.js` - 몬스터 엔티티
| 책임        | 설명                        |
| ----------- | --------------------------- |
| 등급 시스템 | common, rare, epic, legend  |
| 타입 시스템 | attacker, defender, speeder |
| 이동 로직   | 팀에 따라 좌/우 이동        |
| 공격 로직   | 범위 내 적 탐색 및 공격     |
| 상태 관리   | moving, attacking, dead     |

### 6. `entities/Projectile.js` - 투사체 엔티티
| 책임      | 설명                      |
| --------- | ------------------------- |
| 방향 계산 | 시작점→목표점 벡터 정규화 |
| 이동 로직 | 직선 이동 (speed * dt)    |
| 도달 감지 | 목표 지점 도달 여부       |

### 7. `managers/SpawnManager.js` - 소환 관리
| 책임        | 설명                      |
| ----------- | ------------------------- |
| AI 제어     | 난이도별 몬스터 자동 소환 |
| 플레이어 덱 | 7개 슬롯 덱 관리          |
| 비용 관리   | 등급별 에너지 비용        |

### 8. `managers/UIManager.js` - UI 관리
| 책임            | 설명                            |
| --------------- | ------------------------------- |
| DOM 캐싱        | 주요 UI 요소 참조 저장          |
| 실시간 업데이트 | HP, 에너지, 타이머 표시         |
| 덱 UI           | 카드 버튼 생성 및 상태 업데이트 |
| 화면 전환       | 시작, 게임오버 화면 관리        |
| 이벤트 핸들링   | 버튼 클릭 콜백                  |

---

## ⚙️ 상수 및 설정 (`constants.js`)

```javascript
CONSTANTS = {
    GAME: {
        WIDTH: 1280,          // 캔버스 너비
        HEIGHT: 720,          // 캔버스 높이
        FPS: 60               // 프레임 레이트
    },
    CASTLE: {
        MAX_HP: 1000,         // 최대 체력
        ATTACK_RANGE: 500,    // 공격 범위 (픽셀)
        ATTACK_DAMAGE: 20,    // 크리스탈 공격력
        ATTACK_COOLDOWN: 1.5  // 공격 쿨다운 (초)
    },
    ENERGY: {
        INIT: 10,             // 초기 에너지
        MAX: 100,             // 최대 에너지
        REGEN_RATE: 1,        // 일반 재생 (초당)
        BOOST_RATE: 2         // 마지막 30초 재생 (초당)
    }
}
```

---

## 🧩 몬스터 스탯 시스템

### 등급별 기본 스탯

| 등급   | 크기  | 속도 | 공격력 | HP  | 비용 |
| ------ | ----- | ---- | ------ | --- | ---- |
| common | 40×60 | 50   | 20     | 100 | 2⚡   |
| rare   | 50×70 | 40   | 40     | 200 | 4⚡   |
| epic   | 60×80 | 35   | 60     | 350 | 6⚡   |
| legend | 70×90 | 30   | 100    | 500 | 10⚡  |

### 타입별 수정자

| 타입     | 공격력 | HP   | 속도 |
| -------- | ------ | ---- | ---- |
| attacker | ×1.5   | ×0.8 | ×1.0 |
| defender | ×0.8   | ×1.5 | ×0.8 |
| speeder  | ×0.7   | ×0.6 | ×1.8 |

---

## 🔗 의존성 그래프

```
                    ┌────────────┐
                    │  index.html│
                    └─────┬──────┘
                          │ loads
                          ▼
                    ┌────────────┐
                    │   main.js  │
                    └─────┬──────┘
                          │ imports
         ┌────────────────┼────────────────┐
         │                │                │
         ▼                ▼                ▼
   ┌──────────┐    ┌──────────────┐   ┌────────────┐
   │  Game.js │    │SpawnManager.js│   │UIManager.js│
   └────┬─────┘    └──────────────┘   └────────────┘
        │ imports
   ┌────┴────────────┬───────────────┬────────────┐
   │                 │               │            │
   ▼                 ▼               ▼            ▼
┌──────────┐   ┌─────────────┐ ┌───────────┐ ┌─────────┐
│Castle.js │   │ Monster.js  │ │Projectile │ │constants│
└────┬─────┘   └──────┬──────┘ │   .js     │ │  .js    │
     │                │        └─────┬─────┘ └─────────┘
     │                │              │
     └────────────────┴──────────────┘
                      │ extends
                      ▼
               ┌────────────┐
               │  Entity.js │
               └────────────┘
```

---

## 🎯 향후 확장 포인트

### 모바일 지원 (계획됨)
- 반응형 캔버스 시스템
- 터치 이벤트 핸들러
- 세로/가로 모드 레이아웃

### 추가 가능한 기능
| 기능               | 확장 위치                           |
| ------------------ | ----------------------------------- |
| 새로운 몬스터 등급 | `Monster.js` MONSTER_STATS          |
| 새로운 몬스터 타입 | `Monster.js` MONSTER_TYPE_MODIFIERS |
| 아이템 시스템      | `entities/Item.js` (신규)           |
| 스킬 시스템        | `managers/SkillManager.js` (신규)   |
| 멀티플레이어       | `managers/NetworkManager.js` (신규) |
| 사운드             | `managers/SoundManager.js` (신규)   |
| 저장/불러오기      | `managers/SaveManager.js` (신규)    |

---

## 📝 변경 이력

| 날짜       | 버전 | 변경 내용               |
| ---------- | ---- | ----------------------- |
| 2026-02-09 | 1.0  | 최초 아키텍처 문서 작성 |
