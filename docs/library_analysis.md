# 🎮 게임 라이브러리 분석 보고서

## 📋 개요

Castle Defense 게임의 모바일 지원을 위해 사용할 수 있는 라이브러리들을 분석했습니다. 현재 프로젝트는 **순수 JavaScript + HTML5 Canvas**로 구현되어 있으며, 라이브러리 도입 여부를 결정하기 위한 분석입니다.

---

## 🔍 라이브러리 옵션 분석

### 옵션 1: 순수 JavaScript (현재 방식 유지)

| 항목          | 내용                                        |
| ------------- | ------------------------------------------- |
| **장점**      | 의존성 없음, 가벼움, 학습 효과, 완전한 제어 |
| **단점**      | 직접 구현 필요, 개발 시간 증가              |
| **번들 크기** | 0KB (추가 없음)                             |
| **적합성**    | ⭐⭐⭐⭐⭐                                       |

**필요한 직접 구현 항목:**
- 반응형 캔버스 리사이즈
- 터치 이벤트 핸들러
- 좌표 스케일링
- CSS 미디어 쿼리

**추천 이유:** 현재 프로젝트 규모가 작고, 이미 잘 구조화되어 있어 라이브러리 없이 충분히 구현 가능

---

### 옵션 2: Phaser.js

| 항목          | 내용              |
| ------------- | ----------------- |
| **웹사이트**  | https://phaser.io |
| **버전**      | Phaser 3.x        |
| **번들 크기** | ~1MB (minified)   |
| **라이선스**  | MIT               |

**장점:**
- ✅ 가장 인기 있는 HTML5 게임 프레임워크
- ✅ 내장 터치 이벤트 지원
- ✅ 반응형 스케일 매니저 내장
- ✅ 물리 엔진 (Arcade, Matter.js)
- ✅ 풍부한 문서 및 커뮤니티
- ✅ WebGL/Canvas 자동 전환

**단점:**
- ❌ 번들 크기가 큼 (~1MB)
- ❌ 학습 곡선 존재
- ❌ 기존 코드 전면 재작성 필요
- ❌ 프로젝트 규모 대비 과도함

**적합성:** ⭐⭐⭐ (프로젝트 규모 대비 과도함)

---

### 옵션 3: Pixi.js

| 항목          | 내용               |
| ------------- | ------------------ |
| **웹사이트**  | https://pixijs.com |
| **버전**      | Pixi.js v8         |
| **번들 크기** | ~400KB (minified)  |
| **라이선스**  | MIT                |

**장점:**
- ✅ 고성능 2D 렌더링 엔진
- ✅ WebGL 가속 지원
- ✅ Phaser보다 가벼움
- ✅ 스프라이트 관리 용이

**단점:**
- ❌ 게임 로직은 직접 구현 필요
- ❌ 렌더링 전용 (게임 엔진 아님)
- ❌ 터치/입력 시스템은 별도 구현
- ❌ 기존 코드와의 통합 어려움

**적합성:** ⭐⭐ (렌더링만 필요할 때 유용)

---

### 옵션 4: Hammer.js (터치 전용)

| 항목          | 내용                       |
| ------------- | -------------------------- |
| **웹사이트**  | https://hammerjs.github.io |
| **번들 크기** | ~25KB (minified)           |
| **라이선스**  | MIT                        |

**장점:**
- ✅ 터치 제스처 전문 라이브러리
- ✅ 매우 가벼움
- ✅ 탭, 스와이프, 핀치 등 다양한 제스처
- ✅ 기존 코드와 쉽게 통합

**단점:**
- ❌ 터치 이벤트만 처리
- ❌ 캔버스 리사이즈는 별도 구현
- ❌ 게임 개발 목적이 아님

**적합성:** ⭐⭐⭐⭐ (터치만 필요시 좋은 선택)

```html
<!-- CDN 사용 예시 -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/hammer.js/2.0.8/hammer.min.js"></script>
```

---

### 옵션 5: CreateJS (TweenJS/EaselJS)

| 항목          | 내용                 |
| ------------- | -------------------- |
| **웹사이트**  | https://createjs.com |
| **번들 크기** | ~150KB (전체 스위트) |
| **라이선스**  | MIT                  |

**장점:**
- ✅ Adobe 지원 프로젝트
- ✅ 캔버스 객체 관리 용이
- ✅ 애니메이션 트위닝 지원
- ✅ 터치 이벤트 지원

**단점:**
- ❌ 업데이트 빈도 낮음
- ❌ 현대적 ES6 구조와 맞지 않음
- ❌ 기존 코드 구조 변경 필요

**적합성:** ⭐⭐⭐

---

## 📊 비교 요약표

| 라이브러리  | 크기   | 터치 지원 | 반응형    | 학습 곡선 | 통합 용이성 | 추천도 |
| ----------- | ------ | --------- | --------- | --------- | ----------- | ------ |
| **순수 JS** | 0KB    | 직접 구현 | 직접 구현 | 없음      | ⭐⭐⭐⭐⭐       | 🥇      |
| Phaser.js   | ~1MB   | ⭐⭐⭐⭐⭐     | ⭐⭐⭐⭐⭐     | 높음      | ⭐⭐          | ⭐⭐⭐    |
| Pixi.js     | ~400KB | ⭐⭐⭐       | ⭐⭐⭐       | 중간      | ⭐⭐          | ⭐⭐     |
| Hammer.js   | ~25KB  | ⭐⭐⭐⭐⭐     | ❌         | 낮음      | ⭐⭐⭐⭐⭐       | 🥈      |
| CreateJS    | ~150KB | ⭐⭐⭐⭐      | ⭐⭐⭐       | 중간      | ⭐⭐⭐         | ⭐⭐⭐    |

---

## 🎯 권장 사항

### 최종 권장: **순수 JavaScript + 선택적 Hammer.js**

#### 이유:

1. **프로젝트 규모에 적합**
   - 현재 코드 구조가 잘 정리되어 있음
   - 게임 로직이 복잡하지 않음
   - 라이브러리 오버헤드 불필요

2. **학습 효과**
   - 주헌이와 함께 개발하는 프로젝트의 목적에 부합
   - 직접 구현으로 원리 이해

3. **빠른 구현 가능**
   - 반응형 캔버스: ~50줄
   - 터치 이벤트: ~30줄
   - CSS 미디어 쿼리: ~100줄

4. **선택적 Hammer.js**
   - 복잡한 제스처(드래그 앤 드롭)가 필요할 경우만 추가
   - 25KB로 매우 가벼움

---

## 📝 순수 JavaScript 구현 계획

### 필요한 구현 모듈

```
js/
└── utils/
    ├── ResponsiveManager.js    # 캔버스 크기 조절
    └── TouchHandler.js         # 터치 이벤트 처리 (선택)
```

### ResponsiveManager 핵심 로직

```javascript
class ResponsiveManager {
    constructor(canvas, baseWidth = 1280, baseHeight = 720) {
        this.canvas = canvas;
        this.baseWidth = baseWidth;
        this.baseHeight = baseHeight;
        this.scale = 1;
        
        window.addEventListener('resize', () => this.resize());
        this.resize();
    }
    
    resize() {
        const container = this.canvas.parentElement;
        const containerWidth = container.clientWidth;
        const containerHeight = container.clientHeight;
        
        // 종횡비 유지하면서 최대 크기 계산
        const scaleX = containerWidth / this.baseWidth;
        const scaleY = containerHeight / this.baseHeight;
        this.scale = Math.min(scaleX, scaleY);
        
        // 캔버스 CSS 크기 조절 (논리적 크기는 유지)
        this.canvas.style.width = `${this.baseWidth * this.scale}px`;
        this.canvas.style.height = `${this.baseHeight * this.scale}px`;
    }
    
    // 터치/마우스 좌표를 캔버스 좌표로 변환
    toCanvasCoords(clientX, clientY) {
        const rect = this.canvas.getBoundingClientRect();
        return {
            x: (clientX - rect.left) / this.scale,
            y: (clientY - rect.top) / this.scale
        };
    }
}
```

### 터치 이벤트 기본 구현

```javascript
// 터치와 마우스 이벤트 통합 처리
canvas.addEventListener('touchstart', handleStart, { passive: false });
canvas.addEventListener('touchend', handleEnd, { passive: false });
canvas.addEventListener('mousedown', handleStart);
canvas.addEventListener('mouseup', handleEnd);

function handleStart(e) {
    e.preventDefault();
    const coords = getEventCoords(e);
    // 처리 로직
}

function getEventCoords(e) {
    if (e.touches) {
        return { x: e.touches[0].clientX, y: e.touches[0].clientY };
    }
    return { x: e.clientX, y: e.clientY };
}
```

---

## ✅ 결론

| 결정 사항           | 내용                                 |
| ------------------- | ------------------------------------ |
| **주요 방식**       | 순수 JavaScript로 직접 구현          |
| **보조 라이브러리** | 필요시 Hammer.js 추가 고려           |
| **제외된 옵션**     | Phaser.js, Pixi.js (과도한 오버헤드) |

현재 프로젝트의 구조와 규모를 고려했을 때, **라이브러리 없이 직접 구현**하는 것이 가장 효율적입니다.

---

## 📚 참고 자료

- [MDN Touch Events](https://developer.mozilla.org/en-US/docs/Web/API/Touch_events)
- [HTML5 Rocks - Mobile Touch](https://www.html5rocks.com/en/mobile/touch/)
- [Responsive Canvas Tutorial](https://developer.mozilla.org/en-US/docs/Games/Techniques/Crisp_pixel_art_look)
- [Hammer.js Docs](https://hammerjs.github.io/getting-started/)
